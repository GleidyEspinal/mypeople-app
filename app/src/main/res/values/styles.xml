<resources>
    <!-- Tema con ActionBar blanca -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cambia el color de fondo de la ActionBar a blanco -->
        <item name="colorPrimary">@android:color/white</item>
    </style>
</resources>